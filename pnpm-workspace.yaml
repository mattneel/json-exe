packages:
  - "packages/*"
  - "examples"

# esbuild ships a platform binary via a postinstall script; allow it so
# tsup / tsx / vitest work. pnpm blocks build scripts by default.
allowBuilds:
  esbuild: true
